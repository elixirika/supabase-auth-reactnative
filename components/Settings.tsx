import React from "react";
import { View, Text } from "react-native";

const More = ({ route, navigation }: { route: any; navigation: any }) => {
  return (
    <View>
      <Text>Welcome to Settings screen</Text>
    </View>
  );
};

export default More;
